# CSS Style Guide — Project Rules (for qDeveloper)

❗ **Основна проблема**

У проєкті з'явилась велика кількість дублів стилів, що порушує раніше погоджену інструкцію, ускладнює підтримку та призводить до неконтрольованого росту CSS.

**Цей документ — обов'язковий до виконання.**

## 🚫 СТРОГО ЗАБОРОНЕНО

### ❌ !important — ПОВНА ЗАБОРОНА
- Нуль винятків
- Якщо без !important не працює — архітектурна помилка

### ❌ Дублювання стилів
- Заборонено створювати новий клас, якщо існує аналогічний
- Заборонено копіювати стилі між компонентами

### ❌ Inline styles (style="")

### ❌ Стилізація по тегах (div, button, p)

### ❌ Глибокі селектори (.a .b .c .d)

### ❌ Стилі, прив'язані до структури DOM

## ✅ ОБОВ'ЯЗКОВІ ПРАВИЛА

### 1️⃣ ЄДИНЕ ДЖЕРЕЛО СТИЛІВ
- Всі стилі мають бути перевикористовуваними
- Один стиль = один сенс
- Якщо стиль повторюється → він винесений у спільний клас

### 2️⃣ Naming Convention (обов'язково)
Використовуємо BEM-подібну систему:
- `block`
- `block__element`
- `block--modifier`

✅ **Приклад:**
```css
.button {}
.button--primary {}
.button--danger {}
```

❌ **Погано:**
```css
.btn1 {}
.blueButton {}
```

### 3️⃣ Архітектура CSS
```
styles/
 ├── base/        (reset, variables)
 ├── tokens/      (colors, spacing, typography)
 ├── ui/          (buttons, inputs, modals)
 ├── layout/      (grid, wrappers)
 ├── pages/       (page-specific, мінімум)
```

📌 **Page styles — крайній випадок**

### 4️⃣ Design Tokens — ОБОВ'ЯЗКОВО

❌ **Заборонено:**
```css
color: #1f1f1f;
margin: 14px;
```

✅ **Дозволено:**
```css
color: var(--color-text-primary);
margin: var(--space-m);
```

### 5️⃣ Один клас — одна відповідальність

❌ **Погано:**
```css
.card {
  display: flex;
  background: white;
  margin-bottom: 20px;
}
```

✅ **Добре:**
```css
.card {}
.flex {}
.mb-m {}
```

### 6️⃣ Перевикористання ПЕРЕД створенням
Перед додаванням нового стилю:
1. 🔍 Перевір, чи вже існує
2. ♻️ Якщо схожий — розшир, а не дублюй
3. 🧹 Якщо знайшов дублі — рефактори

### 7️⃣ Component-first підхід
- Кожен UI-компонент має один файл стилів
- Жодних локальних хаотичних правил
- Компонент не ламає глобальні стилі

### 8️⃣ Специфічність = мінімальна
**Максимум:**
```css
.block {}
.block__element {}
```

❌ **Заборонено:**
```css
.page .wrapper .content .block {}
```

### 9️⃣ CSS = контракт
- Стиль не змінюється "щоб швидше"
- Тимчасових рішень НЕ ІСНУЄ
- Кожна правка повинна:
  - не ламати існуюче
  - не створювати дубль
  - бути перевикористовуваною

## 🛑 Якщо порушено правила
- PR не приймається
- Код повертається на рефакторинг
- Дублікати — видаляються без обговорення

## 🎯 Ціль
Чистий, масштабований, контрольований CSS без дублів, без !important, без хаосу.